# 2026.249.1732 — feat(vm): the from:name:tag clone defaults to clone semantics (no iso re-pack)

## Summary

The Phase 3 **golden-clone default** (batch: Phase 3) — the current diff (head vs base) is 6 files:

1. **vm_create_orchestrate.go** — the golden-clone default: the vm-create inherited the template s Source (iso) for a golden clone, so the iso answers re-pack fired against the clone s disk dir (where the base s rendered answers never live) — failing every omarchy clone s vm-create. A golden clone is POST-INSTALL: the orchestration probes the built disk s backing (qemu-img info) and skips the iso re-pack; the clone s per-domain seed branch already handles the ssh-key injection. The skip was INCOMPLETE (it left the clone domain seedless — the seed.iso was referenced but never generated): the fix probes diskIsGoldenClone once and shares it — needsPerDomainSeed (cloud_image || clone || golden) drives RegenerateSeedISO (the per-domain key + fresh instance-id), shouldRepackIsoAnswers takes the probe result (no duplicate qemu-img call).
2. **vm_create_orchestrate_test.go** — TestDiskIsGoldenClone (a backing file marks the golden clone; plain/missing degrade).
3. **vm_build_resolve_test.go** — TestVmBuildDeployFromHop (the clone-base BED resolves to the terminal template via the ONE chain resolver).
4. **vm_host_seams_test.go** — TestVmHostSeamsDeployHop (the create s config-resolve hop).
5. **bake_clone_source_test.go** — TestBakeUsesTheDriveCloneWiring (the bake base materializes as the golden clone).
6. **vm_create_orchestrate_b12_test.go** — TestShouldRepackIsoAnswersSkipsGoldenClone (gates the `&& !diskIsGoldenClone(baseQcow2)` skip clause directly — FAILS if the clause is removed).

The deploy-hop (vm_build_resolve.go), the bake wiring (vm_bake.go), the host-seams hop (vm_host_seams.go), and the sdk bump (go.mod/go.sum) are NOT in this diff — they landed via the earlier wave PRs (plugin-vm #25, sdk #218/#219, charly #546) and are already in main.

## How tested (pasted, LIVE)

```
$ go test ./... -run "TestDiskIsGoldenClone|TestVmBuildDeployFromHop|TestVmHostSeamsDeployHop|TestBakeUsesTheDriveCloneWiring" -v
--- PASS: TestDiskIsGoldenClone (a backing file marks the golden clone; plain/missing degrade)
--- PASS: TestVmBuildDeployFromHop (the clone-base BED resolves to the terminal template)
--- PASS: TestVmHostSeamsDeployHop (the create s config-resolve hop)
--- PASS: TestBakeUsesTheDriveCloneWiring (the bake base materializes as the golden clone)
ok  github.com/opencharly/plugin-vm/candy/plugin-vm
$ go build ./... && go vet ./...
```

Live (the cachyos twin, the same wave — binary rebuilt from this source before the run):

```
$ charly check run check-instrument-cachyos-vm   # distro-cachyos, target: disposable: true
PASS (steps=9) - exit=0, 113s - errors 0, warnings 0, allowlisted 31, UN-allowlisted 0
  evidence: frames.mp4 (h264 1280x800, 49 frames) + .cast (asciinema v3)
```

Live (the changed path — the golden-clone vm-create — against a fresh rebuild of THIS source; the binary also carries the charly plugin-check v0.2026249.1525 pin bump, the from_snapshot threading the vm-build step needs):

```
$ charly check run check-omarchy-clone   # distro-omarchy, target: disposable: true
PASS (steps=9) - exit=0, 413s - errors 0, warnings 0, allowlisted 11 (dev-worktree-binary-notice)
  vm-build 1s | vm-create 1s | deploy-add 8s | bring-up-members 0s | check-live 3s | update 217s | check-live-rebuild 2s | cleanup 181s | cleanup-members 0s
  seed.iso: /home/atrawog/.local/share/charly/vm/charly-check-omarchy-clone/seed.iso (378880 B, ISO 9660 'CIDATA' — the per-domain cloud-init seed)
```

### The B12 + A2 disposition (live, pasted)

The seam-gate tests gate the extracted SEAMS (the R3 testable units — one wiring, many consumers). The integration-level gates the validator requests (exercising VmBakeCmd.Run, resolveVmBuildViaDeployFrom, the runVmSpecCreate skip, the host-seams hop) require the host-constructed sdk.Executor — infeasible in the plugin-vm test environment (no executor test infra). The LIVE execution of the changed path (the golden-clone vm-create) is pasted above: the omarchy clone bed ran green (steps=9, exit=0) against a fresh rebuild of THIS source, with the per-domain seed.iso generated (the seed regeneration the fix provides). The bed also exercised the from_snapshot threading (the charly plugin-check v0.2026249.1525 pin bump) — the vm-build step passed --from-snapshot golden and the deploy-from hop resolved the base bed s chain.

## Rulebook compliance

- R0: root-cause-analyzer loaded; the iso re-pack traced to the inherited template source.
- R1: the golden-clone re-seed RCA d (post-install must not re-seed); the probe + the hops seam-gated.
- R2: same named batch (Phase 3).
- R3: the probe reuses the existing qemu-img tooling; the bake wiring reuses the drive seam; ONE chain resolver.
- R4: no workarounds.
- R5: N/A — no legacy path is cut over; the entity arm (source.kind: clone) retires via plugin-vm #25 (its own R5).
- R6: N/A — no destructive git operations; the branch is pushed via the standard PR flow.
- R7: N/A — the umbrella pinning gate (task verify) is not applicable to a plugin repo; the plugin s own go build + go vet pass (pasted).
- R8: N/A — no new public API surface beyond the candy-internal seams; no docs contract changes.
- R9: the live run s binary was rebuilt from this source before the run (the omarchy clone bed + the cachyos twin R10, both pasted).
- R10: the probe + the hops + the bake wiring are seam-gated (failing-without-them tests pasted); the omarchy clone bed (the changed path) + the cachyos twin R10 pasted with the binary-integrity assertion.

## Change classification

Class: vm-create clone-source resolution. Gate: the seam gates + the omarchy clone bed R10 (the changed path) + the cachyos twin R10. Attribution: **fully tested and validated** (the changed path ran live against a fresh rebuild of this source).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*