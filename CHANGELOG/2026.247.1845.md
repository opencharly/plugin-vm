# 2026.247.1845 — fix(vm): metadata-only snapshot delete + non-GPU claimants never attach a stale hostdev

## Summary

Two measured fixes in plugin-vm (head 6d91fd5):

1. **Snapshot delete uses METADATA-ONLY** via the snapshotDeleteFlags() helper — `charly vm snapshot delete` took >2 min because a full delete unlinks the snapshot disk (the running keep_venue domain's in-use backing); libvirt metadata-only returns in 14 ms. charly owns the disk lifecycle (the caller removes the per-snapshot dir), so the helper returns libvirt.DomainSnapshotDeleteMetadataOnly at all three call sites.
2. **Non-GPU claimants never attach a stale instance hostdev** — autoAllocateExclusiveGPUs now DROPS stale <hostdev>s (capturing the count first) when the claimant needs no GPU; GPU claimants untouched.

## How tested

3 unit tests (real output):
```
note: test-vm does not require a GPU; dropped stale <hostdev>s (was 1)
--- PASS: TestAutoAllocate_DropsStaleHostdevForNonGPUClaimant
--- PASS: TestAutoAllocate_KeepsHostdevForGPUClaimant
--- PASS: TestSnapshotDeleteFlags_MetadataOnly   (fails if the flag is reverted to 0 — B12)
```

LIVE snapshot-delete proof — the DOMAIN-PRESENT branch (the changed DomainSnapshotDelete(snap, snapshotDeleteFlags()) call actually EXECUTES; fresh-rebuild binary 2026.247.1634; named bed check-snap-probe, disposable: true; the golden was provisioned by the bed's fresh lane and the keep_venue domain was RUNNING):
```
$ time charly vm snapshot delete snap-probe-vm golden --domain check-snap-probe --force
deleted snapshot "golden" on vm "snap-probe-vm"
real 0m0,159s
```
No "Domain not found" line — the domain was found and the metadata-only delete executed; post-state verified (libvirt snapshot list empty — the metadata was removed). Pre-fix the same delete hung >2 min (measured earlier, killed).

The GPU-stale-hostdev fix pairs with plugin-check#19 (the runner auto-provisions the missing base golden).

Rulebook: R1 (root-caused, measured); R3; R4 (no workarounds); B12 (the flag test); R7 pasted; B17 (named disposable bed). Tier: fully tested and validated.

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (fully tested and validated)*