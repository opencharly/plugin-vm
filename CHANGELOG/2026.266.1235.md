# 2026.266.1235 — feat(plugin-vm): adopt spec 261 (schema-head pin bump)

## Summary

The schema-249→261 wave leg for `plugin-vm` (the `charly vm` command / `kind:vm` provider) — a prerequisite for the engine-provider-class cutover (spec#144/#145). `plugin-vm` self-loads project config through `sdk/loaderkit`, whose `GateSchemaVersion` rejects a config NEWER than the plugin's compiled HEAD.

**Complete diff accounting:** `go.mod` changes ONLY the two pins (`spec v0.2026259.2033 → v0.2026266.545`, `sdk v0.2026260.1434 → v0.2026266.623`); `go.sum` changes ONLY the matching four lines. No `golang.org/x/*` hunk; no code migration needed.

## How tested

**Change-class gate:** `charly check run check-substrate` (vm, `disposable: true`) — its `vm-build`/`vm-create` run through this plugin against a 261 project. Branch-built engine-cutover charly `2026.266.0826`.

**Full, unedited step list (12 steps, all `PASS`):**
```
[vm-build] PASS after 20.1s
[vm-create] PASS after 21.829s
[deploy-add] PASS after 54.786s
[bring-up-members] PASS after 35.85s
[check-live] PASS after 35.479s
[rebuild-members-down] PASS after 23.639s
[update] PASS after 2m28.065s         # fresh rebuild (R10)
[re-bring-up-members] PASS after 35.238s
[rebuild-ready] PASS after 931ms
[check-live-rebuild] PASS after 32.154s
[cleanup] PASS after 26.494s
[cleanup-members] PASS after 28.447s
PASS (steps=12)
```
`.check/check-substrate/2026.266.1115/summary.yml` → `ok: true`, `errors: 0`, `warnings: 0` on every step.

**Disposable target:** `check-substrate` (`disposable: true`); self-destructs in `cleanup`.
**Fresh rebuild:** `[update] PASS 2m28.065s` + `[check-live-rebuild]`.
**Changed-path execution:** the `vm-build`/`vm-create` + entity-resolution paths (the migrated plugin) ran live.

Local gates: `gofmt -l .` (empty) · `go build ./...` · `go vet ./...` · `go test ./...` GREEN.

## Project-rulebook rule-compliance

- **R0** — check/plugin/git-workflow skills loaded first.
- **R1** — failures were root-caused to the schema skew; fixed in-tree.
- **R2** — wave legs named on opencharly/charly#633.
- **R3/R4** — pin adoption only; no duplication/shim.
- **R5** — no removed identifier.
- **R6/R7** — green build/vet/test + the real bed.
- **R8/R9** — no artifact change; the bed runs the branch binary `2026.266.0826`.
- **R10** — the `check-substrate` disposable fresh-rebuild cycle, 0 warnings / 0 failed steps.
- **RDD** — the schema gate proven fixed live.
- **ADE** — the candy's baked plan ran.
- **Disposable-only autonomy** — only the bed's own teardown.

## Change classification

Dependency/pin bump in a plugin candy (`go.mod`/`go.sum` only). Verification gate: the `check-substrate` disposable R10 bed (fresh rebuild) + local Go gates. Attribution tier: `fully tested and validated`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*