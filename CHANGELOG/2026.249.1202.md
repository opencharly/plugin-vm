# 2026.249.1202 — feat(vm): the from: name:tag drive hops a DEPLOY target through the base bed

## Summary

The **from: name:tag drive** (Phase 3): `charly vm build <entity> --from-snapshot <tag>` builds the entity as a CLONE of its own golden at that snapshot, and the drive HOPs a DEPLOY target through the base bed (the golden registry lives under the base bed's name): the build's entity-resolve follows the chain (sdk's loaderkit.DeployTargetEntity — the ONE resolver, released) to the terminal template while from_vm stays the requested name (the registry owner). The create's config-resolve got the same hop (this branch). The source.kind: clone entity arm + charly vm clone retire (R5). Pins: sdk v0.2026249.1109 (the released hop + the ONE-resolver chain), spec v0.2026249.855 (the plugin-vm s own pin - the VmBuildRequest.from_snapshot schema, #99) — NO local replaces (the published module graph).

**`charly vm bake` change (accounted):** the bake base now materializes as a clone of the entity OWN golden at a REQUIRED --from-snapshot <tag> (the retired entity-clone source made the snapshot implicit; the flag makes it explicit - bakeRequiresSnapshot replaces the deleted requireCloneSource guard). The bake base clone-source wiring uses the SAME extracted seam as the build drive (applyCloneDriveSource - R3, one wiring two consumers) + is unit-gated (TestBakeUsesTheDriveCloneWiring); the bake command has no production consumer in the bed roster, so the wiring is verified at the unit seam (R10) rather than in a live bed.

## How tested (LIVE, pasted)

The drive ran LIVE on the converted instrument bed (check-instrument-cachyos-vm — disposable: true; binary built from the wave tree with the published module graph):

```
$ charly check run check-instrument-cachyos-vm    # run 2026.249.1032
[vm-build] PASS — vm-build.log:
  Building VM "check-vm-clone-base" (source.kind=clone)
  Wrote output/qcow2/check-vm-clone-base/disk.qcow2 (clone of check-vm-clone-base@golden)
$ go test -run 'TestVmBuildDriveSourceKind|TestApplyCloneDriveSource|TestBakeRequiresFromSnapshot|TestKnownVmSourceKinds' -v .
--- PASS (all — the drive decision, the source wiring, the guards)
$ go build ./... && go mod verify    # against the published sdk/spec (no replaces)
```

## Rulebook compliance

- R0: git-workflow + strict-policy loaded.
- R1: pasted live output; the drive + the hop executed on the real golden snapshot.
- R2: same named batch (Phase 3); the loader split + hop + ONE-resolver (sdk #217/#218/#219), the schema (#99-#102), the runner (#26) are the other legs.
- R3: ONE drive (BuildClone), ONE chain resolver (DeployTargetEntity), ONE retirement error.
- R4: no workarounds, no local replaces — the pin resolves the published module (real go.sum entries).
- R5: the entity arm + charly vm clone + the dead writers DELETED in this branch.
- R10: the changed drive branches are unit-gated + ran live (pasted).

## Change classification

Class: VM build/create drive (functional cutover). Gate: unit gates (every changed branch incl. the bake wiring) + the live drive run + released-chain build. Attribution: analysed on a live system (the vm-build drive ran live with pasted output; the bake Run() path is unit-gated but did not execute live - the honest A3 tier).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*