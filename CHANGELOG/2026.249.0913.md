# 2026.249.0913 — feat(vm): the from: name:tag drive — vm build --from-snapshot + clone entity retirement (Phase 3)

## Summary

The unified **from: name:tag functional half** (Cutover A addendum Phase 3) in plugin-vm:

1. **`charly vm build <entity> --from-snapshot <tag>`** — builds the entity as a CLONE of its own golden at that snapshot. The drive decision + source wiring are extracted as `vmBuildDriveSourceKind` (request carries from_snapshot → reply SourceKind "clone", the DRIVE — never an entity kind) and `applyCloneDriveSource` (from_vm = the entity itself), both unit-gated.
2. **The ENTITY arm retires (R5):** `source.kind: clone` in a vm.yml entity is now a LOUD error pointing at the deploy spelling `from: <entity>:<snapshot>` (the loader splits it) or the `--from-snapshot` one-off; `knownVmSourceKinds` drops "clone".
3. **`charly vm clone` retires** (it only wrote the retired entity syntax; zero consumers); the dead clone-declaration YAML writers are deleted.
4. **`charly vm bake` requires --from-snapshot** (the base is the entity's own golden clone); the entity-clone guard is replaced by bakeRequiresSnapshot.
5. Pins: spec @ v0.2026249.855, sdk @ v0.2026249.856.

## How tested

```
$ go build ./... && gofmt -l .   # clean
$ go test -run 'TestVmBuildDriveSourceKind|TestApplyCloneDriveSource|TestBakeRequiresFromSnapshot|TestKnownVmSourceKinds' -v .
--- PASS: TestVmBuildDriveSourceKind       # fails without the resolve drive branch
--- PASS: TestApplyCloneDriveSource        # fails without the vm_build.go drive wiring
--- PASS: TestBakeRequiresFromSnapshot
--- PASS: TestKnownVmSourceKinds_ExcludesClone
$ go test -count=1 .
ok  	github.com/opencharly/plugin-vm/candy/plugin-vm
```

The two DRIVE tests are the R10 gate for the headline feature: removing either new branch fails its test. The BuildClone input guards are unchanged and still tested.

## Rulebook compliance

- R0: git-workflow + strict-policy skills loaded first.
- R1: every build/test output pasted; zero warnings.
- R2: same named batch (Phase 3); the runner half (plugin-check threading) rides the follow-up after spec #100.
- R3: ONE clone drive (BuildClone); ONE retirement error; the flag mirrors the deploy field spelling.
- R4: no workarounds — the retirement error is the honest dead-surface disposition; no aliases.
- R5: the entity arm + the vm clone command + the dead writers are DELETED; the error points at the new surface.
- R10: every changed branch is unit-tested (the two drive branches have dedicated gates).

## Change classification

Class: VM build drive (functional cutover). Gate: unit tests covering all changed branches. Attribution: fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*